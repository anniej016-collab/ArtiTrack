/**
 * How many songs can be picked out of one release.
 *
 * Three is the point of the feature rather than a technical limit: a shortlist
 * that can hold everything is just the tracklist again. Fewer than three is
 * always fine, and none at all is the normal state — nothing anywhere asks you
 * to pick.
 */
export const MAX_FAVOURITE_SONGS = 3;
